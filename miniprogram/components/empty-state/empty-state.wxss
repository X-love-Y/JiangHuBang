.empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 0;
}
.empty__icon {
  font-size: 88rpx;
  opacity: 0.5;
  margin-bottom: 24rpx;
}
.empty__text {
  font-size: 26rpx;
  color: var(--ink-3);
  letter-spacing: 2rpx;
}
